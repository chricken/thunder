'use strict';

const settings = {
    elements: {},
    paused:false,
    potts:[],
    delayNewLightning: 1000,
    player:false,
}

export default settings;
export const elements = settings.elements;